# Cucumber

## Usage
```bash
# setup into given folder
java -jar Cucumber pack.zip -o path/to/mod-pack/folder

# TODO possible idea
# creates the `pack` folder in the parent
# like how a project creates its folder in the parent
java -jar Cucumber pack.zip -o path/to/parent
```
